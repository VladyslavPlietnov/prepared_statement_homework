package org.example.objects;

public class ProjectWorker {
    public int projectId, workerId;

    public ProjectWorker(int projectId, int workerId) {
        this.projectId = projectId;
        this.workerId = workerId;
    }
}
